# Structura — Claude Instructions

## Working Directory

Always work directly in the main repository at `/Users/daniel/Documents/GitHub/Structura`. Do **not** use worktree isolation (`isolation: "worktree"`) when spawning subagents — it makes the dev server inaccessible for testing. Edit files in-place on the current branch.

## Dev Server

Run `npm run dev` from the project root. The app runs at `http://localhost:3000`.
